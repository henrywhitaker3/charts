{{- if and (.Values.vpn.healthcheck.enabled) (.Values.vpn.enabled) }}
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: {{ include "generic.fullname" . }}-healthcheck
spec:
  itemPath: {{ .Values.vpn.healthcheck.secret }}
{{- end }}

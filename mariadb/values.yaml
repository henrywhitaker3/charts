generic:
  nameOverride: mariadb
  fullnameOverride: mariadb

  replicaCount: 1

  image:
    repository: mariadb
    pullPolicy: Always
    tag: latest

  deploymentStrategy:
    type: Recreate

  service:
    portName: mysql
    port: 3306

  ingress:
    enabled: false

  pvc:
    enabled: true
    mountPath: /data
    storageClassName: longhorn
    size: 10Gi

  probes:
    type: tcpSocket
    options:
      port: mysql
